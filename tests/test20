--TEST--
Tests the error package.

--INPUT--
using('error');

return [
    error.New("hello"),
    error.IsError(error.New("hello")),
    error.IsError("hello")
];

--RESULT--
[hello, true, false]