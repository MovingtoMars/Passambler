--TEST--
Tests ternary expressions.

--INPUT--
import('std.Str');

a = 1 + 1;

return [
    a == 2 ? "1 + 1 == " + Str(a) : "1 + 1 != 2",
    true ? "hello" : "no hello"
];

--RESULT--
[1 + 1 == 2, hello]